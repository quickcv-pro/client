import React from "react";

const InterestDesign = (props) => {
  return (
    <div className="interestContainer">
      <p className="interestText">{props.interest}</p>
    </div>
  );
};

export default InterestDesign;
